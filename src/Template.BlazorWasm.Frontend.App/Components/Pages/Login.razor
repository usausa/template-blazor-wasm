@page "/login"

<PageTitle>ログイン - テンプレート</PageTitle>

<div class="login-container">
    <FluentCard Width="360px">
        <FluentStack Orientation="Orientation.Vertical" VerticalGap="12">
            <FluentLabel Typo="Typography.H4">ログイン</FluentLabel>
            <FluentTextField @bind-Value="name" Label="ID" Class="full-width" />
            <FluentTextField @bind-Value="password" TextFieldType="TextFieldType.Password" Label="パスワード" Class="full-width" />
            @if (errorMessage is not null)
            {
                <FluentMessageBar Title="@errorMessage" Intent="MessageIntent.Error" AllowDismiss="false" />
            }
            <FluentButton Appearance="Appearance.Accent" Loading="@processing" OnClick="OnLoginClickAsync" Class="full-width">ログイン</FluentButton>
        </FluentStack>
    </FluentCard>
</div>
